
// we have to make different roles 

const authRoles={

    // we define it as key value if in future we change the value the key will remain same

    ADMIN:"ADMIN",
    MODERATOR:"MODERATOR",
    USERS:"USERS",
}

export default authRoles;